// Package events will be populated in T17 with the four real event types of
// EventHub and their process registrations. It exists now as a buildable
// placeholder so the dispatch tree compiles with go build ./...
package events
